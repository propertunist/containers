<?xml version="1.0" encoding="UTF-8"?>
<plugin_manifest xmlns="http://www.elgg.org/plugin_manifest/1.8">
	<name>Containers</name>
	<author>ura soul</author>
	<version>0.4.1</version>
	<category>enhancement</category>
	<blurb>Add selector to assign items to a container/group</blurb>
	<description>allows entities to be assigned to groups/containers - such that they can be moved from one group to another. also adds a label to entity description outputs to show which group the entity is contained by.</description>
	<website>http://www.infiniteeureka.com/</website>
	<license>GNU General Public License version 2</license>
	<requires>
		<type>elgg_release</type>
		<version>1.8</version>
	</requires>
	  <requires>
    <type>plugin</type>
    <name>groups</name>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>au_sets</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>blog</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>blog_tools</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>file_tools</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>pages_tools</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>bookmarks</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>file</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>pages</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>tidypics</plugin>
  </requires>
  <requires>
    <type>priority</type>
    <priority>after</priority>
    <plugin>videolist</plugin>
  </requires>
</plugin_manifest>
